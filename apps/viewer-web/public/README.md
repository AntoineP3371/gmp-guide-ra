`manifest.example.json` est une **copie** de `../../../examples/manifest.example.json`, servie
ici statiquement pour que le champ d'URL par défaut du viewer fonctionne sans configuration
(Vite ne sert pas de fichiers hors de la racine du projet). Si tu modifies l'exemple canonique,
recopie-le :

```bash
cp examples/manifest.example.json apps/viewer-web/public/manifest.example.json
```
